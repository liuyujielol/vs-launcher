export type WIDTHS_TYPES = "sm" | "md" | "lg" | "xl" | "2xl" | "fit" | "full"
export const WIDTHS = {
  sm: "w-7",
  md: "w-14",
  lg: "w-28",
  xl: "w-44",
  "2xl": "w-56",
  fit: "w-fit",
  full: "w-full"
}

export type HEIGHTS_TYPES = "sm" | "md" | "lg" | "xl" | "2xl" | "fit" | "full"
export const HEIGHTS = {
  sm: "h-7",
  md: "h-14",
  lg: "h-28",
  xl: "h-44",
  "2xl": "h-56",
  fit: "h-fit",
  full: "h-full"
}

export type BG_COLORS_TYPES = "light" | "dark" | "vs"
export const BG_COLORS = {
  light: "bg-zinc-800",
  dark: "bg-zinc-850",
  vs: "bg-vs"
}

export type TEXT_COLORS_TYPES = "light" | "dark" | "vs"
export const TEXT_COLORS = {
  light: "text-zinc-200",
  dark: "text-zinc-500",
  vs: "text-vs"
}

export type OPEN_DIRECTIONS_TYPES = "left" | "right" | "top" | "bottom"
